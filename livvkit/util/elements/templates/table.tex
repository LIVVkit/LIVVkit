\begin{table}[h!]
    \centering
    \begin{tabular}{cc}
        \BLOCK{ for column in data['data'].keys() }\VAR{ column }\BLOCK{ if not loop.last } & \BLOCK{ endif }\BLOCK{ endfor } \\
        \hline
        \BLOCK{ for row in range(rows) -}
        \BLOCK{ for column in data['data'].keys() }\VAR{ data['data'][column][row] }\BLOCK{ if not loop.last } & \BLOCK{ endif }\BLOCK{ endfor }  \\
        \BLOCK{ endfor }\end{tabular}
\end{table}