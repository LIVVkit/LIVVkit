\begin{table}[h!]
    \centering
    \begin{tabular}{\BLOCK{ if index is not none } r | \BLOCK{ endif }\BLOCK{ for column in data['data'].keys() }c\BLOCK{ endfor }}
        \BLOCK{ if index is not none } & \BLOCK{ endif }\BLOCK{ for column in data['data'].keys() }\VAR{ column }\BLOCK{ if not loop.last } & \BLOCK{ endif }\BLOCK{ endfor } \\
        \hline
        \BLOCK{ for row in range(rows) -}
        \BLOCK{ if index is not none }\VAR{index[row]} & \BLOCK{ endif }\BLOCK{ for column in data['data'].keys() }\VAR{ data['data'][column][row] }\BLOCK{ if not loop.last } & \BLOCK{ endif }\BLOCK{ endfor }  \\
        \BLOCK{ endfor }\end{tabular}
\end{table}